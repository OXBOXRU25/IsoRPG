# IsoRPG — состояние

Обновлено 01.09.2026. Версия 0.8.0. Следующая задача — физика препятствий (см. последний раздел).

## Что это

Изометрическая РПГ на Unity 6.5 (6000.5.9f1), URP 17. Игра называется
**Adventures of Zhenya**. Репозиторий `OXBOXRU25/IsoRPG`, ветка `main`.

## Как я работаю с проектом

**Unity гоню я сам, Павлон только смотрит результат.** Список «нажми то,
потом это» — откат, а не норма.

```
node ~/.claude/scripts/unity-compile.mjs "D:/GAME Ai/IsoRPG"     # проверка кода, 3 с
```

Пакетный прогон заданий (редактор должен быть закрыт):

```
printf 'задание\n' > IsoRPG/_pending-tasks.txt
"C:/Program Files/Unity/Hub/Editor/6000.5.9f1/Editor/Unity.exe" -batchmode -quit \
  -projectPath "D:/GAME Ai/IsoRPG" \
  -executeMethod IsoRPG.EditorTools.BatchRunner.Run \
  -logFile "D:/GAME Ai/unity-batch.log"
```

**Собрал — сразу запусти сам**, доведи до арены (Enter в меню) и оставь
работать, пока Павлон не закроет. Фокус на окно наводить ДО нажатия Enter,
иначе оно уходит в чужое окно.

**Сборка занимает файл игры.** Пока игра открыта, пересобрать нельзя.

## Как собирать мир (итог ночи 30.08.2026)

**Мир собирать ОДНИМ циклом, а не по кускам.** Точечные прогоны за ночь
трижды разносили состояние: то вода исчезала, то трава возвращалась к
старым настройкам. Полная цепочка:

```
relief → pond → meadow-ground → meadow-synty-clear → meadow-synty →
nobatch → sit → conform → water-fix → world-check → build
```

**`world-check` ставить ПОСЛЕДНЕЙ строкой в каждый прогон.** Он читает
состояние сцены и печатает контрольный список: террейн, слои земли,
водоёмы, растения, волки, герой, навигация. Ругается, если чего-то нет.
Журнал сборки печатает тот же код, который делал работу, и подтверждает
лишь то, что код дошёл до строки, — этому верить нельзя.

**ПОСЛЕ ЛЮБОГО ИМПОРТА НАБОРА** прогонять заново:

```
node tools/patch-vegetation-shader.mjs   # изгиб травы в шейдере
```

и задание `conform`. У наборов Synty общая папка `PNB_Core`, и каждый пакет
несёт её целиком: импорт соседнего биома **перезаписывает шейдер
растительности и стирает правку**. Симптом — трава разом перестаёт гнуться,
а `conform` отвечает «включено 0 материалов»: свойства в шейдере больше
нет. Проверка одной строкой:

```
grep -c "_PNBHeightMap" "IsoRPG/Assets/PolygonNatureBiomes/PNB_Core/Shaders/SyntyStudios_VegitationShader.shader"
```

Ноль — правку съело импортом. То же касается настроек импорта текстур
(`mipcover`): всё, что лежит в чужих папках, живёт до следующего импорта.

**Вода переведена на URP заданием `water-fix`.** Родной
`SyntyStudios/WaterShader` написан под встроенный конвейер и в URP не
рисуется ВООБЩЕ — не пурпурным, а невидимым, поэтому пропажу воды легко
принять за потерю объектов. Доказывается подменой материала (`water-mat`).
Гладь сейчас однотонная прозрачная: в родном материале обычной текстуры нет
вовсе, только карты искажения, пены и преломления — вид создавал сам шейдер.
Для нормальной воды ставить **Stylized Water 2** (лежит в `H:\3`, сделан под
URP) или чинить шейдер набора конвертером.

**Габариты любого нового вида мерить ДО посева** заданиями `grass-shot`
(объект рядом с эталоном роста 1.8 м) и `pine-probe`. За ночь дважды
обожглись: `Grass_Plane` оказались плоскостями, лежащими на земле, а
`Ground_Cover` с плотностью 8.4 на 100 м² — зарослями выше героя.

**Ёлки альпийского набора не годятся:** у них хвоя со снежными кончиками,
летом читается как иней. Набор `PNB_Alpine_Mountain` при этом установлен, и
альпийская трава оттуда полезна — она втрое уже луговой (1.65 м против 4.6)
и на склонах не отрывается.

## Задания очереди

Мир: `relief` (рельеф + пересев луга + навигация), `pond` (водоёмы),
`meadow-synty` (посев луга), `meadow-ground` (земля со слоями и тропами),
`terrain-repair` (пересоздать карту высот, если данные террейна пропали),
`build` (собрать игру).

Мобы и герой: `hero-num` (поставить героя по файлу выбора), `anims-native`
(родные клипы Synty в наш контроллер), `wolves` (стая волков).

Щупы: `nav-hole` (карта дыр в навигации вокруг точки), `grip-probe` (хват
оружия вариантами в ряд), `weight` (состав сцены числами), `nav` (сетка),
`sky-probe`, `pond-probe`, `pnb-analyze` и `pnb-meadow` (разбор демо-сцен
Synty на числа), `pond-shot` (кадры из редактора).

Прочее: `hud` (вернуть интерфейс герою), `audio` (вернуть звуковой узел),
`minimap`, `music`, `synty-sky`, `menu`.

**`mipcover` прогонять после каждого разворачивания наборов.** Папка
`Assets/TriForge Assets` лежит в `.gitignore`, поэтому настройка импорта
текстуры в репозиторий НЕ попадает — в git живёт только само задание. Без
прогона цветы снова начнут пропадать при приближении.

**`meadow-synty-clear` сносит НЕ ТОЛЬКО луг.** 30.08.2026 я гонял пару
`meadow-synty-clear` + `meadow-synty` десяток раз подряд, подбирая траву, и
каждый раз вместе с растениями уходили водоёмы — заказчик увидел кольцо
кувшинок, лежащее на траве без воды. В журнале всё выглядело исправно:
пруды честно копались на следующем полном прогоне. **Правило: после любой
очистки — щуп состава сцены, а не доверие журналу.** И не гонять очистку
ради подбора одного числа: дешевле собрать варианты в один прогон.

**Порядок для мира строгий: `relief` → `pond` → `meadow-ground` → `build`.**
Копка чаши не идемпотентна: каждый запуск `pond` вычитает глубину заново,
и пруды уходят всё глубже. Рельеф надо лепить начисто перед копкой.

## Норматив автора набора — снят щупами 30.08.2026

**Главный вывод ночи: почти всё, чем мы «чинили» траву, было нашим
изобретением, которого у автора набора нет.** Каждая починка лечила симптом
предыдущего изобретения. Числа сняты заданиями `grass-norm` и `tree-norm` с
демо-сцен `PNB_Meadow_Forest/Scene/Demo.unity` и `PNB_Enchanted_Forest`.

- **наклон**: автор кладёт куст по нормали склона — средний наклон 16.7° при
  средней крутизне места 17.3°, строго вертикальных 1%. 57% его травы стоит
  на склонах круче 15°, полного размера. У нас стоял `PlantTilt = 0`, и
  вертикальный широкий куст на склоне лечь не может физически — отсюда
  «висит краем» и «торчит кончиками», которые лечились чем угодно, кроме
  причины. Теперь `PlantTilt = 1`;
- **пересечения**: у автора 3.08 пары на куст, 201 пара глубже 70%, есть
  совпадающие полностью. Наши 6727 пар были нормой жанра, а не поломкой;
  разрежение съедало треть посева — отсюда «луг редковат». `OverlapShare`
  поднят с 0.55 до 0.85;
- **размеры — распределением, а не краями**. Это была главная ошибка: с демо
  сняли «масштаб 0.60–2.92» и разыгрывали РАВНОМЕРНО, а у автора середина
  0.99 и девять десятых не выше 1.16, предел 2.92 — выброс на две тысячи
  кустов. Средний куст выходил вдвое крупнее авторского. В таблице теперь
  стоят его десятая и девяностая доли по 18 видам; потолок `GrassMaxScale`
  убран за ненадобностью (он был заплаткой, случайно возвращавшей среднее);
- **раскладка**: автор засевает 15 300 м² из 160 000 пятнами по 5.2 куста на
  100 м². Введены 60 пятен радиусом 9 м, 72% посева в них;
- **изгиб травы в шейдере ВЫКЛЮЧЕН** заданием `conform-off`. У автора его нет
  вовсе. Именно он поднимал траву полосой над гребнем склона — жалоба
  «трава висит в небе», которая держалась двое суток;
- **заглубление деревьев**: автор топит берёзу на 1.05 м (10% высоты, 38
  экземпляров), луговое дерево на 1.49 м, великанов на 2.4–3.1 м. Наша
  посадка «нижней точкой на грунт» давала ноль, и весь корневой раструб
  оставался на поверхности — дерево стояло на паучьих лапах.

## Крупные деревья

Задание `big-trees` (плюс `trees-shot` для кадров). 48 штук: два великана
Зачарованного леса (61 и 64 м), `Tree_Large_01/02` по три, луговых 16,
берёз 24. Правила в коде, каждое проверяется замером после посадки:

- **сажаем по нижней точке МОДЕЛИ и топим на глубину раструба корней.**
  Раструб считается по геометрии: нижняя половина дерева режется на 40 слоёв
  и ищется высота, где поперечник перестаёт превышать ствол. Для великанов
  это 8.5–9.6 м, а авторская доля дала бы всего 2.2 — оба числа нужны,
  берётся большее;
- водоём обходится по кроне (у великанов — по стволу плюс 10 м: их крона на
  шестидесяти метрах и над водой не мешает);
- общий список занятых мест включает деревья, уже стоящие на лугу;
- растения, оказавшиеся внутри ствола, удаляются;
- после посадки — перепечка навигации, иначе сквозь стволы можно пройти.

## Две арены: наша и мир автора

**`Assets/_Game/Scenes/Arena.unity`** — наш мир. **`ArenaAuthor.unity`** — копия
арены, в которой снесено наше окружение и развёрнут мир автора целиком.
Обе живут файлами, переключение одним заданием, пересобирать нечего:

```
arena-clone        # копия Arena.unity → ArenaAuthor.unity (копия ФАЙЛА)
author-world       # снести наше окружение, развернуть мир автора, нав
arena-use-author   # кнопка «Начать игру» ведёт в ArenaAuthor
arena-use-ours     # вернуться на нашу арену
arena-open-author  # открыть копию, чтобы следующие задания шли в неё
```

**`arena-open-author` ставить ПЕРВОЙ строкой**, если правишь копию: пакетный
запуск всегда открывает нашу арену, и задание, отработавшее не в той сцене,
выглядит успешным и не меняет ничего.

Что снесено на копии: террейн, `Луг Synty`, `Деревья`, `Пруд Synty`,
`Небо Synty`. Что осталось: герой, волки, интерфейс, звук, камера, свет —
вместе с `GroundHug` и `GroundAlign`, они висят на самих существах. Герой и
волки поставлены на грунт автора (иначе висели бы на высотах снесённой
земли). Навигация печётся по его земле: 417 × 427 м.

### Цепочка сборки мира автора — гонять ЦЕЛИКОМ

После любого отката сцены `ArenaAuthor` прогонять всё подряд, а не по
кускам: правки лежат в разных заданиях, и вспоминать их по памяти — это
ровно то, на чём я попался 30.08.2026, дважды подряд отдав заказчику мир то
без неба, то без света.

```
arena-open-author     # открыть копию, ИНАЧЕ всё уйдёт в нашу арену
sky-reimport          # небо автора заново из его сцены
sky-upper-off         # выключить верхний купол (синий диск)
backdrop-off          # погасить фоновые холмы-обманку
author-light-use      # его солнце вместо нашего + туман и рассеянный свет
author-repair         # потерянные материалы и облака на URP
materials             # общая URP-починка
water-fix             # вода Synty на URP (родной шейдер невидим)
underwater            # двусторонняя гладь + подводный туман
arena-use-author      # кнопка «Начать игру» ведёт сюда
build
```

**`arena-use-author` ставить ПОСЛЕ `menu`**, если меню пересобиралось:
пересборка меню заново прописывает, в какую сцену ведёт кнопка, и затирает
выбор арены. Симптом — «запустил, а там старый мир».

**Небо автора — два вложенных купола, верхний надо выключать.** Оба
`SM_Env_Skydome_01` лежат в `Background_Sky` с одним материалом
`Skybox_Meadows_Mat_01` на шейдере `SyntyStudios/SkyboxUnlit`. Нижний (639 м
высотой) рисуется в URP верно — розово-голубой градиент с облаками. Верхний
(1760 м) даёт сплошную тёмно-синюю заливку: шейдер строит градиент по
мировой высоте, и вся видимая часть верхнего купола уходит за его край.
Лечится заданием `sky-upper-off` — выключением объекта, БЕЗ правки
материалов. Правка материалов здесь только ломает: перевод купола на URP
стирает текстуру, и небо становится плоским силуэтом.

Мир автора требует после разворачивания: `materials` (URP-починка),
`water-fix` (родной водный шейдер в URP невидим), `synty-sky` (наше небо
снесено вместе с землёй, а оно к земле отношения не имеет — ставится его
собственный `Skybox_Meadows_Mat_01`).

**Не доделано на копии:** водяное колесо мельницы покрашено материалом воды —
`water-fix` берёт объекты по материалу, а автор нарисовал колесо на том же.
Сузить отбор до плоскостей глади. Небо закатно-розовое (его собственное);
если нужно дневное — вернуть наш купол.

## Сцена автора рядом с нашей

Задание `author-scene` ставит демо-сцену лугового биома целиком на **400…800
по X**, в ста метрах за нашим восточным краем (наш террейн кончается на 300).
Живой эталон: дойти пешком и сверить. Задание `nav-bake` печёт сетку на обе
земли — без неё туда не дойти.

**Три грабли этой задачи, все об одно:** сдвиг считался от нуля координат, а
не от края нашей земли; равнялся по габаритам сцены, в которых лежит задник
горизонта в 4.5 км (земля уехала за два с половиной километра); край нашей
земли мерился ДО сноса прошлой копии, отчего каждый прогон отодвигал сцену
ещё на триста метров. Задник теперь выбрасывается (всё шире километра), свои
камеры и светильники тоже.

## Мир сейчас

Луговой биом Synty. Числа сняты щупом с демо-сцен автора, не выдуманы.

- **рельеф**: перепад 20 м, крутизна средняя **11.9°**, наибольшая **48.6°
  в точке (47, −19)**. Три слоя шума (60 / 20 / 6 м), маска плоскостей даёт
  около 40% ровных мест, одна доминанта на горизонте. Мелкий слой раньше
  дублировал средний (15.3 против 17 м) — отсюда была «одеяльная» карта;
- **водоёмы четыре**: (20, −16) гладь 13, (−46, 34) гладь 29, (60, 58)
  гладь 35, (−62, −66) гладь 36. Берег гуляет заливами: край чаши
  колеблется от 1.18 радиуса глади до полной чаши. Уже глади не станет
  никогда, иначе вода вылезет на сушу — модель глади круглая;
- **стая волков** у малого пруда, три штуки. Собраны в три этажа: корень
  с агентом → узел наклона (`GroundAlign`) → модель с аниматором;
- **земля**: шесть слоёв лугового биома, плитка 2–4 м. Доли автора: трава
  80.5%, палая листва 6.5%, земля 2.1%, галька 2.0%, трава с листвой 1.0%,
  тропа 0.2%;
- **тропы**: слоем террейна, три извилистых линии шириной 2.5 м, огибают
  водоёмы;
- **растительность**: 1628 растений 18 видов, 6116 объектов с LOD-узлами;
- **водоёмы**: малый (20, −16) гладь 13 м, большой (−46, 34) гладь 29 м.
  Чаша копается, потом наливается вода, кувшинки только на глади;
- **небо**: купол `SM_Env_Skydome_01` с материалом `Skybox_Mat_02`, два
  облачных кольца, облака идут. Купол на слое `Sky`, выведен из-под солнца.

- **герой** прижат к грунту компонентом `GroundHug`: сетка навигации лежит
  выше земли, и агент честно ставил персонажа на неё. Правится `baseOffset`
  агента каждый кадр, наклона нет — человек на склоне стоит вертикально;
- **цветы** не пропадают при подходе: у атласа были включены мип-уровни без
  сохранения покрытия альфы, тонкий лепесток проваливался сквозь порог
  отсечения. Лечится заданием `mipcover`, **прогонять после каждого
  разворачивания наборов** — папка наборов в `.gitignore`.

Сцена весит около 5 МБ (была 151 МБ до чистки).

## Что не доделано

0. **Невидимая стена — ЗАКРЫТО 29.08.2026.** Крутизна рельефа доходила до
   **48.6°** при пределе подъёма агента **45°**: сетка на той полосе не
   строилась, игрок утыкался в её край. По решению Павлона `agentSlope` в
   `ProjectSettings/NavMeshAreas.asset` поднят до **55**. Дыр вокруг (44, −21)
   было 13 из 169, стало 1.

   **Кайма холмов как граница мира — ОТВЕРГНУТА, не предлагать снова.**
   Пробовали по образцу WoW: дно выположить до 32°, по краю вал 40 м шириной
   и 24 м высотой с кубическим профилем. Числа вышли верные, мир — нет:
   «мир стал плоским, ощущение, что ты в чаше». Причина в масштабе: карта
   400 м и камера изометрическая, кольцевой вал виден со всех сторон разом и
   читается как борт миски. Код (`LimitSlope`, `Relax`, `AddRim`) остался в
   истории git — писать заново не надо.

1. **Трава на склонах — РАБОТАЕТ, осталась вкусовая настройка.**
   «Висит», «тонет», «торчит кончиками» оказались следствиями четырёх разных
   причин, и лечились они порознь:

   - **габариты куста несовместимы с рельефом.** Меш 4.6 м в поперечнике при
     авторском масштабе до 2.54 давал куст в одиннадцать метров при высоте
     метр — плита такого размера не ложится на склон никак. Введён потолок
     `GrassMaxScale` 1.3, подбор размера под крутизну (`ConformLimit` делить
     на тангенс) и нижняя граница 0.8 от авторского минимума;
   - **изгиб по рельефу в шейдере.** `TerrainConform` отдаёт карту высот
     глобальной текстурой, шейдер опускает вершину туда, где под ней земля,
     относительно центра куста. Предел изгиба 1.5 м. Правка шейдера
     воспроизводится `tools/patch-vegetation-shader.mjs`: сам шейдер лежит в
     игнорируемой папке набора, копия до правки — рядом с ним
     (`.before-conform`);
   - **статический батчинг ломал изгиб.** Трава была помечена ПОЛНОЙ статикой,
     объединение мешей переводит вершины в мировые координаты и лишает куст
     своей матрицы — шейдер брал опорную высоту из центра батча, и трава
     улетала аркой в небо. Задание `nobatch` снимает только флаг объединения;
   - **кусты сидели друг в друге** (догадка Павлона, подтверждена замером):
     из 1628 пересекались 6727 пар, 651 глубже 70%, худшая на 99%. Введено
     разрежение при посеве, `OverlapShare` 0.55, список занятых мест общий на
     весь посев (внутри цикла по видам он сравнивал куст только с сородичами).
     Компенсация плотности — `SowExtra` 1.6.

   Замер из игры: посажено 1551, земли не нашли 0, утоплено глубже 10 см — 77
   (было 627), наибольший сдвиг 1.14 м (было 3.59), глубоких наложений 0.

   **Что осталось: луг на вкус редковат.** Попытка добавить виды из набора
   провалилась дважды, и оба раза из-за того, что я не мерил габариты до
   посева. `Grass_Plane` оказались плоскостями, лежащими на земле (объёма не
   дают, видны только края). `Ground_Cover` с суммарной плотностью 8.4 на
   100 м² превратили луг в заросли выше героя. **Прежде чем добавлять вид —
   снять его габариты щупом `grass-shot`** (он рендерит объект рядом с
   эталоном роста 1.8 м и печатает высоту и ширину).

2. **Угол водной глади вылезает из-под земли.** Модель воды у Synty
   неровная, её дальние выступы уходят за выкопанную чашу. Нужно копать
   полную глубину заведомо шире глади — но осторожно, 29.08 я на этом
   сломал карту высот целиком.
3. **Мерцание тени** на близкой камере — старая жалоба, см. память проекта.
4. **Лицензия музыки**: на первом экране стоит строка про Kevin MacLeod
   (CC BY 4.0), а музыка теперь другая. Строку надо менять, лицензия нового
   набора неизвестна — ждёт решения Павлона.

## Наборы

**SIDEKICK — конструктор персонажей.** Поставлен 29.08.2026: стартовый
набор из `H:\2\Hero`, плюс инструмент и база, скачанные с GitHub Synty
(`SyntyStudios/SidekicksToolRelease`, копии лежат там же в `H:\2\Hero`).
Открывается меню `Synty → Sidekick Character Tool`. Частей всего 58 — два
набора, Fantasy Knights и Sci-fi Civilians; остальные восемь наборов в базе
числятся, но мешей к ним нет, это платные паки. Собранный герой
выгружается кнопкой `Export Character as FBX` и попадает своей папкой в
корень `Assets`. Сборка склеивается в ОДИН меш: снять оружие или часть
из готовой модели нельзя, только пересобрать в конструкторе и выгрузить
заново.

**Герой сейчас — `Human-Custom2`** (`Assets/Human-Custom2`), собран
Павлоном в Sidekick. Рецепт сборки лежит рядом файлом `.sk`, из него
герой восстанавливается кнопкой `Load Character`.

Установлены и в `.gitignore` (восстанавливаются из `H:\1` и `H:\2`
командой `node tools/import-unitypackage.mjs <путь>`):

- **PolygonNatureBiomes** — заколдованный лес, луговой, болота, тропики.
  938 МБ. Внутри лежал вложенный вариант под URP
  (`URP_ExtractMe/Unity_2022.unitypackage`), поставлен поверх;
- COZY 3 (небо и погода) — 517 МБ, встроенный пакет в `Packages/`. **На
  второй машине его надо доставить руками**, иначе Package Manager ругнётся
  на неразрешённую зависимость. Сейчас в сцене не используется — небо
  сделано куполом Synty;
- Blink, ExplosiveLLC, GSpawn, Layer Lab, PolysplitGames, VFX_Klaus.

**Правка чужих шейдеров.** В `PNB_Core/Shaders` у `SyntyStudios_WaterShader`
и `WaterScrolling` снято семь объявлений `_CameraDepthTexture_TexelSize` в
каждом: URP 17 объявляет её сам, и своё объявление ломало компиляцию. При
переустановке набора правку придётся повторить.

## Звук и музыка

- банк звуков Kenney (CC0): бой, шаги, интерфейс, предметы, голоса, птицы;
- заставка — `Action 3 Loop`, зациклена;
- в игре — десять дорожек `Ambient_*` вперемешку из `Resources/Music`,
  пауза между темами 6 с, вход и выход плавные;
- слушатель звука **на герое**, не на камере: в изометрии камера в двадцати
  метрах, и звук рядом с героем доходил бы десятой долей.

## Грабли, на которые уже наступали

- **Строитель СТАРОЙ песочницы** вешал на героя миникарту, настройки,
  панель кнопок, подсказки, журнал, шаги и слушателя, а также собирал
  звуковой узел. Арену собирал другой строитель — всё это приходилось
  возвращать по частям заданиями `hud`, `audio`, `minimap`;
- **`SaveOpenScenes` пишет только открытые сцены.** Задание правит арену,
  следующее задание в том же прогоне открывает другую сцену — правка
  арены теряется. Разводить по отдельным прогонам;
- **выключить объект недостаточно**: `SetActive(false)` гасит отрисовку и
  скрипты, но объект остаётся в файле и создаётся при загрузке;
- **шейдер судить по тегам, а не по имени**: `SyntyStudios/SkyboxUnlit`
  объявлен как обычная геометрия, в `RenderSettings.skybox` не встаёт;
- **одна правка — один взгляд.** 29.08 три правки в одном коммите скрыли,
  какая из них сломала карту высот. Откат сломал ещё больше.

## Заход 31.08–01.09.2026: бой, квест, анимации — и упор в физику

**Главный вывод ночи: мы третий раз подряд лечим симптомы одной болезни.**
Залезание в мобов, невидимые стены, кривой прыжок, застревание на камнях —
это не четыре бага, а одно следствие: **движение героя живёт на
навигационной сетке, а физики препятствий в мире нет вовсе.** Замер
01.09.2026 по `ArenaAuthor.unity`: сеточных коллайдеров **ноль**, коробчатый
один, капсульных 25 — и все 25 это сами существа. Камни, заборы и стены
физически не существуют; есть картинка и запечённая карта проходимости,
которая строится прямо по мешам, минуя физику.

**План на 02.09.2026, согласован с Павлом (три шага):**

1. **коллайдеры на мир** — заданием, автоматически. Камням коробки, деревьям
   и кустам капсулы, сеточные только там, где без них никак: сеточный
   коллайдер на тысячах объектов уронит производительность;
2. **герой с NavMeshAgent на CharacterController** — штатная капсула Unity с
   гравитацией и высотой шага, ровно вовская схема. Мобы остаются на
   навигации, им физика не нужна;
3. **переписать под него**: ходьбу на клавишах, подход к собеседнику и
   сундуку, прижим к грунту (станет не нужен), разведение тел (станет
   физическим само собой).

Цена — день-полтора, и на это время ходьба героя будет сломана. Зато уходят
разом невидимые стены, залезание в мобов, кривой прыжок и застревание.

### Что сделано и работает

- **весь игровой набор героя перенесён на арену автора** заданием
  `player-kit`: выбор цели, бой, характеристики, сумка, экипировка, таланты,
  квесты, окна, экран смерти с кнопкой. Было 12 компонентов, стало 48.
  **Причина, по которой ничего не выделялось: на герое не было
  `PlayerInputRouter`** — единственного места, где читается мышь. Искали в
  коллайдерах мобов и слоях, а клик не обрабатывал никто;
- **мобам донесено недостающее** заданием `mob-kit`: обработка смерти
  (без неё убитый моб продолжал бить), оглушение, полоска над головой только
  враждебным, возрождение, радиус тела по фактической модели;
- **бой оживлён** заданием `combat-anims`: серия из шести ударов кинжалом
  вместо одного, вздрагивание от попаданий у героя И у зверей (клипы нашлись
  у обоих наборов), жесты способностей (направленный, по площади, усиление),
  уклонение, крадущийся шаг для скрытности, полупрозрачность в скрытности,
  прыжок тремя фазами;
- **способность «Спринт»** — кнопка 4, +70% скорости на 15 с, откат 2 мин,
  с 5-го уровня, из тени не выводит. Ускорение отдельным компонентом
  `SpeedBoost`;
- **квест «Слишком много кабанов»** у Талина Кини: 12 клыков (падают с
  каждого кабана гарантированно), 250 опыта, 85 золота, **награда на выбор**
  из двух штанов — механика выбора сделана, кнопки в окне сдачи;
- **костёр в лагере** — огонь и дым, эффекты Synty переведены на URP;
- **лагерь**: NPC Талин Кини стоит на точке Павлона лицом к котелку, рядом
  вторая лошадь (масть «в яблоках»).

### Что НЕ работает и ждёт физики

- герой **залезает в кабана** (по указанию Павла разведение у героя
  выключено совсем: он стоит намертво, отходит моб — проверить утром);
- **прыжок**: перенос через препятствие есть, но ведёт себя странно —
  перелетает низкие камни, затаскивает на высокие, с камня не слезть;
- **невидимые стены** — край карты проходимости;
- **репутация** — механики нет вовсе, 250 из награды нигде не учитываются;
- **блок и уклонение** — клипы и состояния готовы, механики нет.

### Ловушки, на которые наступили этой ночью

- **сброс прогресса не работал трижды.** Игра при старте **переносит
  сохранение из папки прежнего названия** — `LocalLow/OXBOX/Птица высокого
  полёта/character.json`. Удалять надо ОБА файла и **при закрытой игре**:
  запущенная перезапишет сейв при выходе;
- **`Animals Packs` у Malbers содержит модели кабана.** Я спрятал папку
  вместе со скриптами, которые не собираются под Unity 6, — и кабаны стали
  невидимыми (коллайдер бьёт, модели нет). Скрипты Malbers лежат в `Common~`
  и `Animal Controller~` (суффикс `~` исключает папку из импорта), шейдеры
  возвращены отдельно в `MalbersShaders`;
- **переимпорт `.unitypackage` затирает `Packages/manifest.json`** — проект
  разом лишился URP и Input System. `tools/import-unitypackage.mjs` научен
  пропускать манифест;
- **состояние Death строилось без выхода** у всех: возрождённый моб бегал за
  героем лёжа. Добавлен переход по снятому флагу;
- **у состояний из Any State обязателен выход** — иначе персонаж залипает в
  позе удара после смерти цели.

### Новые задания

`player-kit`, `mob-kit`, `combat-anims`, `abilities`, `items`, `campfire`,
`campfire-off`, `fire-row`, `fire-probe`, `fx-urp`, `camp-horse`,
`horses-show`, `horse-skins`, `camp-shot`, `mob-height`.

**`world-check` теперь проверяет и боевую комплектность:** ругается, если у
кого-то нет обработки смерти, у героя нет компонента ввода или экрана
смерти. Заведено потому, что одну и ту же недостачу пришлось лечить дважды.

## Заход 01.09.2026, вторая половина: физика, окно персонажа, голоса

**Физика героя переведена на капсулу.** `PlayerMotor` — `CharacterController`
с гравитацией; навигационный агент остался считать путь, но позицию не
трогает (`updatePosition = false`). Ставится и снимается заданиями
`hero-physics` / `hero-physics-off`. Прыжок стал настоящим: толчок вверх, а не
перенос по сетке.

**Проход сквозь существ.** Капсула игнорирует коллайдеры живых тел: разовый
проход при старте плюс страховка по событию столкновения. Периодических
переборов в игре НЕТ — первая версия перебирала 2077 коллайдеров каждые две
секунды и подняла игру колом («приложение не отвечает»).

**«Лошадь отталкивается» — это была навигация, а не физика.** У существ стоял
`HighQualityObstacleAvoidance`, и они сами уступали дорогу. Снято заданием
`no-avoidance` по ВСЕМ агентам сцены и в `BodySpace` при старте. Журнал
доказал непричастность физики: капсула лошадь не касалась ни разу.

**Кольцо вокруг цели** (`CombatRing`): нападающие занимают восемь секторов
вокруг жертвы и держат место. Раньше все целились в одну точку, и босс вставал
на мелкого кабана.

**Живое тело больше не считается землёй** — в `GroundAlign`, `GroundHug`,
`GroundSitter`. До этого моб вставал на спину соседа, и оба уезжали в воздух.

**Обеих лошадей собирает один код** (`CampHorse`), прежний `HorsePack` из
употребления убран. Контроллер и оба материала масти готовятся ОДИН раз до
сборки: создание удаляет старый ассет, и вызов внутри цикла оставлял
предыдущую лошадь без контроллера (камень) или без материала (пурпурная).

**Окно персонажа перестроено по образцу WoW** — замеры сняты с его скриншота:
окно 490×600 (в образце 484×603), гнёзда 53 с рамкой, две колонки по краям (8
и 8), модель по центру в полный рост, числа под ней двумя колонками по пять
строк без прокрутки. Цвета замерены: панель (19,19,18), заголовки (221,221,221),
подписи (202,190,107), значения (210,221,208), шаг строки 21.

**Иконки слотов** нарезаны из сеток Павлона (`Icons/`), 16 подложек в
`Resources/SlotIcons` и 4 предмета в `Resources/ItemIcons`. Выровнены по фону,
свету и контрасту скриптами в scratchpad. Импорт в спрайты — задание
`slot-icons`.

**Голос НПС** (`NpcVoice`): случайная фраза из `Resources/Voice/Npc`, не та же
подряд, откат от конца реплики, громкость 0.55. Старое «бормотание» из окна
разговора убрано.

**Слоты экипировки расширены** до вовского набора: плечи, запястья, пояс,
колчан, второй аксессуар, герб, рубашка. Дописаны В КОНЕЦ перечисления —
значения хранятся числами.

**Не доделано:** в окне персонажа нет портрета в шапке, кнопок поворота модели
и панели сопротивлений; репутация и блок/уклонение по-прежнему без механики.

**Два прогона Unity нельзя пускать внахлёст.** Очередь `_pending-tasks.txt`
читается и ОЧИЩАЕТСЯ первым же запуском: второй получает пустой список, молча
собирает игру со старой сценой и рапортует об успехе. 01.09.2026 из-за этого
казалось, что правки звука и НПС не работают — они просто не доехали до сцены.
Признак: в журнале нет строк «Выполняю задание», только «Пакетный запуск:
готово». Проверять этим грепом, прежде чем искать причину в коде.

## Заход 01.09.2026, вечер: камера и вздрагивание — ЗАКРЫТО

Все четыре пункта подтверждены Павлоном в собранной игре.

**Камера проваливалась в геометрию** (запрыгнуть на камень и развернуться —
экран заливает текстурой). Причина: луч защиты стартовал из `smoothedFocus`
— сглаженной точки, которая догоняет героя с задержкой. На прыжке она
отставала и оставалась ВНУТРИ камня, а изнутри коллайдера физика попаданий
не видит: защита слепла ровно в тот кадр, когда была нужна. Лечится одной
строкой — стрелять от истинного положения героя. Прошлые две попытки
(двигать точку взгляда; выталкивать камеру циклом `CheckSphere`) лечили
следствие; вторая к тому же не могла работать — у камней Synty сеточные
коллайдеры, а у треугольной сетки нет «внутри». Обе сняты.

**Камера считала препятствием живых** («прохожу через вторую лошадь — камера
делает полный зум»). Маска была `~0`. Теперь отбор СЛОЕМ: живые переведены
на `Characters` заданием `creature-layer` (25 тел, 1467 объектов), слой
исключён из препятствий вместе с водой, небом, травой и интерфейсом.
Признак живого — навигационный агент ИЛИ скелетная сетка: отбор по одному
агенту пропускал лошадь-декорацию.

**Камера больше не приближается вообще** — решение Павлона: «за деревьями
она просто не должна менять ракурс и зум». Выключено полем `dodgeObstacles`
в `IsoCameraRig`. Повод виден в замере щупа `cam-block`: у деревьев Synty
коллайдер накрывает крону целиком (`Tree_Fruit_01` — 2.7×3.7×2.5 м при 382
штуках, `Tree_Meadow_02` — 5.8×5.5×6.3 м), и шаг в сторону заводил луч в
листву.

**Стартовый угол камеры** 50° → **140°** (`yaw` в `ArenaAuthor.unity` и в
компоненте). Снят с кадра Павлона двумя способами: по конусу на мини-карте
и по тому, где лежит водоём.

**Вздрагивание от попаданий** ломало анимацию кабанов — числа и разбор в
памяти проекта (`combat-flinch`). Кратко: реакция длилась 1.42 с при ударе
героя раз в 1.4 с, то есть зверь был в ней всё время боя. Теперь 0.40 с,
задаётся СЕКУНДАМИ (задание `flinch-tune`), и не прерывает собственный удар.

### Новые задания

`creature-layer` (живые на слой Characters), `flinch-tune` (длительность
вздрагивания), `boar-probe` (щуп: клипы, длины, переходы, интервалы),
`cam-block` (щуп: чем зажимается камера, по видам и размерам коллайдеров).

### Грабли этого захода

- **Задание обязано САМО открывать нужную сцену.** `creature-layer` в первом
  прогоне покрасил старую `Arena.unity` — она осталась открытой с прошлого
  раза — и отчитался «тел 4, без слоя никого». Отчёт честный, только про
  чужую сцену: в боевую не попало ничего, а `build` открыл её сам и собрал
  непокрашенной. Зелёная проверка на пустом множестве.
- **Долю клипа нельзя применять ко всем.** `flinch-tune` сперва ставил всем
  выход на 25%: кабану (клип 1.63 с) дало нужные 0.41 с, а герою (0.50 с) —
  0.13 с. Задавать надо секундами. Поймал повторный щуп в той же очереди.
- **Красить ветку существа надо целиком, а не «где слой Default».** У каждого
  существа висит капсула `Head` из набора Malbers на своём слое — двадцать
  голов остались препятствием для камеры.

## Что не доделано после этого захода

1. **Герой не виден за деревом.** Прямое следствие отключённого приближения:
   камера держит ракурс, а крону не убирает. Правильный ответ — не возвращать
   наезд, а растворять то, что закрыло героя, и включить силуэт: код лежит в
   `SilhouetteSetup`, проход рендера с него снят.
2. **Скалы и холмы автора — меши.** Камера теперь может войти внутрь них,
   и тогда вернётся «изнанка на весь экран». В игре пока не всплывало.
3. **Клип `Boar Get Hit` зациклен** — ошибка настройки импорта в наборе
   Malbers. При коротком выходе не видна; правка слетит при переустановке.
4. **`world-check` на арене автора всегда ругается** «ЧЕГО-ТО НЕ ХВАТАЕТ»:
   он считает наши посевы, которых в мире автора нет (растений 0). Щуп надо
   научить различать миры, иначе он приучает не смотреть на свою ругань.
5. **Выпадение дропа помечено розовым** — заглушка Павлона, ждёт нормального
   вида.

## Заход 02.09.2026: гриб-босс, анимации, портреты, мигание травы

**Мигание растительности при отдалении камеры — ЗАКРЫТО решением Павлона.**
Мигало переключение уровней детализации: на дальних уровнях у куста просто
меньше цветков, так его упростил художник. Вся растительность переведена на
упрощённые модели (`maximumLODLevel: 1` в профиле PC) — мигать стало нечему,
и это втрое дешевле: уровень 1 стоит 28% треугольников от подробного, уровень
2 — 12%. Плюс `CalmDistantWind` гасит качание при отдалении: вблизи полная
сила, вдали почти штиль (Павлон: «издалека в глазах рябит»).

Отвергнуто замерами по дороге, не повторять: мелочь террейна (её нет),
вырезание на дальнем уровне, каскады теней, покрытие альфы в мип-уровнях,
статический батчинг (снят давно), GPU-отрисовщик (выключен), предел
видимости `NearOnly` — он режет слой `Detail`, а на нём **ноль объектов**.

**Гриб-исполин** на (−43, −113), уровень 6, в засаде: стоит обычным грибом,
оживает в семи метрах. Все 20 клипов набора InfinityPBR. Задания
`mushroom-boss`, контроллер собирает `MushroomAnimations`.

**Босс-кабан** переведён на все 36 клипов набора: семь ударов, боевая стойка,
вздрагивание с четырёх сторон, кружение, покой, оглушение, прыжок.

**Праздное поведение** (`IdleBehaviour`, задание `idle-kit`): зверь ест,
садится, спит вне боя. Раздаётся по признаку «контроллер понимает параметр
Rest».

**Голоса** (`VoiceKit`, задание `voice-kit`): волки рычат, кабаны хрюкают,
главарь рычит по-кабаньи — звуками Павлона вместо старого рыка скелета.

**Портреты** нарисованы Павлоном: герой, НПС, кабан, вожак, волк, гриб.
Съёмка с моделей отвергнута (см. память `portraits-not-rendered`).

### Грабли этого захода

- **Размер навигационного агента задаётся по СЕТКЕ, а не по туше.** Сетка
  построена под радиус 0.5 и рост 2 м; агент шире полигона постоянно
  оказывается за краем, и Unity возвращает его — зверь прыгает назад.
- **`SaveOpenScenes` пишет только грязные сцены**, а `AddComponent` из кода
  сцену грязной НЕ помечает. Задания отчитывались об успехе, в сцену не
  попадало ничего. Проверять сам файл сцены, а не отчёт.
- **Число, заданное и в коде, и в сцене, работает из сцены.** Правка кода не
  догоняет уже расставленный компонент.
- **Лишний пустой аниматор на корне** (от `RequireComponent`) перехватывает
  любой поиск «первого аниматора в ветке»: настоящий живёт на модели.
- **Модель, положенная в сцену как экземпляр FBX-префаба, не держит правок**
  (контроллер, аватар) — распаковывать `UnpackPrefabInstance`.
- **У Generic-скелета без аватара анимации молчат**, зверь едет по земле.

### Не доделано

Все четыре пункта закрыты 02.09.2026, разбор ниже.

## Заход 02.09.2026, вечер: анимации зверей, хват кинжала, звук гриба

**Кабану и волку розданы полные наборы.** Задание `beast-anims` — оно
пересобирает контроллеры и раздаёт их существам НА МЕСТАХ, не пересобирая
стаи: пересборка вернула бы им уступание дороги, снятое `no-avoidance`.

- **кабан: 5 клипов было, 48 стало.** У Malbers их 68, и лежат они таками
  ВНУТРИ шестнадцати FBX — по списку папки набор выглядит бедным, и именно
  поэтому мы полгода брали из него четыре штуки. Взято: 4 удара плюс
  разгонный (`BAttackRun`, входит по `Speed > 2.2`), 4 вздрагивания по
  сторонам, разворот на месте, двумерное дерево хода (шаг/рысь/бег с
  наклонами и задний ход), покой — еда, питьё, сидение, сон с переходными
  клипами, два разных падения по жребию. Не взято и почему: плавание (9 —
  воды под ногами не бывает), падение с высоты (5 — нет обрывов), маски (3 —
  для добавочных слоёв), парные смерти `Opp` (2 — для добивания вдвоём),
  развороты на 180°, запечённые прыжки, два клипа встряхивания;
- **волк: 5 было, 13 стало** из 15. Три атаки вместо одного укуса,
  вздрагивание, прыжок, еда, отдых, «озирается» и **вой**. Не взят задний
  ход: скорость приходит модулем, клип был бы недостижим;
- **пороги хода сняты С САМИХ КЛИПОВ**, а не подобраны. Замер: у кабана шаг
  0.87, рысь 1.94, бег **6.34 м/с**, а стояло «бег с 3.0» — при скорости
  агента 3.0 зверь всё время играл полный бег, то есть ноги перебирали вдвое
  быстрее, чем он ехал. Это и есть скольжение по земле. Мерится смещением
  корня за клип, делённым на длину; у Generic-скелета кривых трансформа нет,
  там берётся `clip.averageSpeed`. Корень ищется по НАИБОЛЬШЕМУ нетто-смещению,
  а не по наименьшей глубине пути: у волка верхний узел только качается, и по
  глубине замер давал ноль. Общая мера — `ClipSpeed.Measure`, одна на всех;
- **у волка клип бега медленнее, чем он бегает**: 2.47 м/с против 3.6 у агента.
  Порог поднят до боевой скорости, а клип растянут во столько же раз
  (`ChildMotion.timeScale`), иначе лапы отстают от земли почти наполовину. У
  кабана обратное — бег 6.34 при агенте 3.0, там дерево само даёт нужную
  видимую скорость смесью рыси и бега;
- **`Boar Get Hit` расцикливается заданием** (пункт «клип зациклен» из
  прошлого захода). Правка живёт в настройках импорта, папка Malbers в
  `.gitignore` — после переустановки набора прогнать `beast-anims` заново.

**Три дырки, найденные попутно щупом `beast-probe`:**

1. **бой слал номер удара от 1 до 6 всем подряд.** Константа в
   `MeleeCombatant`. У кабана ударов 4, у волка 3, у гриба 3 — номера сверх
   их числа не совпадали ни с одним переходом, и удар шёл БЕЗ анимации: у
   гриба половина ударов, у кабана каждый третий. Теперь число берётся у
   водителя анимаций, а тот получает его от задания сборки (и умеет
   пересчитать сам по состояниям `Attack_N`);
2. **поза оглушения не играла ни у кого** — состояние в контроллере босса
   собрано, а флаг `Stunned` не поднимал никто. Поднимает `StunReceiver`;
3. **второе падение гриба не включалось** — `DeathVariant` не ставил никто.
   У гриба он значит «расплющен тяжёлым» и жребию не подлежит, а у кабана
   два равнозначных падения, и там жребий уместен (`deathVariants` = 2).

**Занятия вне боя опознаются по имени состояния.** Входное состояние каждого
занятия зовётся `Rest_N` — по нему `IdleBehaviour` узнаёт, что зверь умеет.
Раньше список стоял числом в компоненте и всем врал одинаково. Задание может
проставить список точно, опрос остаётся общим правилом на остальных.

### Хват кинжала — четыре причины под одним симптомом

**Числа из Blender к делу не подошли ВООБЩЕ**, и это главный вывод: посадка
оружия считается по костям кисти прямо в Unity (`grip-fit`), а не переносится
из редактора моделей. Ниже — почему.

**Первая: у Sidekick есть авторский стандарт крепления, и мы им не
пользовались.** У скелета лежат кости-держатели `prop_l` / `prop_r`, а кроме
них `backAttach`, `hipAttach_l/r`, `hipAttachBack/Front`, `shoulderAttach_l/r`,
`elbowAttach_l/r`, `kneeAttach_l/r`, `headAttach`, `faceAttach` — под щит за
спину, ножны на пояс, наплечники. **Мерили от `hand_r`, а `WeaponVisual`
находит `prop_r` первым** — все довороты применялись к другой кости, смещённой
на 7.5 см вперёд. Отсюда «кинжал проходит сквозь начало кисти».

**Вторая: в игре был не тот кинжал.** Каталог ссылался на `SM_Prop_Dagger_01`
из набора персонажей, `ItemsBuilder` по умолчанию отдаёт `SM_Wep_Dagger_01` из
набора оружия, а примеряли и утверждали `SM_Wep_Dagger_02`. Три модели с
разной опорой. `grip-fit` переводит кинжалы каталога на утверждённую, отбирая
по слову «Dagger» в имени модели, а не по одному известному имени.

**Третья: кисти у Sidekick НЕ зеркальны.** Щуп `hand-probe` намерил
расхождение отражённой правой и левой в **204° по трём осям**. Отражение
(`MIRROR @ local @ MIRROR`), которым мы получали левую руку, врало по
построению. Павлон увидел это глазом раньше замера: «странно что кинжалы
по-разному легли, логично делать один и другой отражать».

**Четвёртая: углы Эйлера переносили перестановкой компонент** — у Blender
вертикаль Z и порядок XYZ, у Unity Y и ZXY, перестановка повороту не равна.

**Как считается теперь.** Всё выводится из скелета, подбирается одно число:

- **ось рукояти** — линия оснований пальцев (`index_01` → `pinky_01`): у
  сжатого кулака рукоять лежит вдоль неё. Клинок смотрит наружу со стороны
  указательного;
- **точка хвата** — середина между основанием пальца и его кончиком,
  усреднённая по четырём пальцам, **в СЖАТОМ кулаке** (замер идёт по позе из
  `Armed-Idle`). Кончики там возвращаются к ладони, и середина попадает ровно
  внутрь захвата. Первый заход мерил по раскрытой ладони и добавлял
  догаданные 12 мм — Павлон сразу увидел итог: «рукоять выходит за внешние
  пределы руки, надо сажать глубже в ладонь». У раскрытой ладони захвата нет
  вовсе, добавлять к ней нечего;
- **разворот лезвия** — плашмя к ладони: гарда вдоль направления пальцев;
- **оси самой модели** меряются по мешу (клинок 0.712 м по локальной Y, гарда
  0.186 по Z), поэтому разница импортёров в расчёт не входит;
- **каждая рука считается по СВОИМ костям**, отражения нет вовсе.

Итог: правая `-0.0357 / 0.0108 / -0.0109`, доворот `6.1 / 340.6 / 176.8`;
левая `-0.0357 / 0.0108 / 0.0109`, доворот `6.1 / 199.4 / 183.2`.

**Проворот клинка вокруг своей оси** — правка Павлона по кадру: «кинжал лежит
плашмя, развернуть примерно наполовину». Стоит 45°, задаётся полем `Roll`.
Знак для левой руки **обратный** (`LeftRollSign`): проворот идёт в системе
модели, а базисы рук зеркальны по смыслу, и один и тот же угол крутит клинки в
противоположные стороны относительно тела. Симптом был ровно такой: «только
левый кинжал повернулся, надо так же правый». Глубина сверх расчёта — 5 мм
(`DepthNudge`). Щуп `grip-probe` умеет ставить ряд по провороту: шесть
положений от −45° до +90° одним кадром.

**Полоска над головой вдвое тоньше** (0.13 → 0.065) и под ней **ряд комбо-очков
из пяти фрагментов**: фон тот же, что у здоровья, набранные загораются жёлтым.
Показывается только на той цели, где очки есть, — пустой ряд у каждого моба
был бы мусором. Число фрагментов берётся у самой механики (`ComboPoints.
MaxPoints`), а не написано пятёркой. Размеры проставляет `mob-kit`: у
расставленных в сцене полосок лежат прежние числа, и правка умолчания в коде
их не догоняет.

**Праздное поведение обрывалось мозгом зверя.** Павлон: «звери буквально
ложатся на секунду и встают». Длительность занятия была ни при чём — она и
была 5–12 с. `MonsterBrain.Patrol` каждые полсекунды выдавал новую точку
прогулки, зверь трогался, а `IdleBehaviour` видел «идёт» и бросал занятие.
Теперь прогулка на время покоя держится (`idle.Resting`), движение мешает
только НАЧАТЬ занятие, а не обрывает начатое. Длительность поднята до 14–28 с:
цепочка «сесть — лечь — спать» с подъёмом съедает секунд шесть, и при
двенадцати зверь едва успевал лечь.

**Пальцы — отдельная задача, доворотом оружия не чинится.** Герой играет
безоружные стойки Synty, в них ладонь раскрыта. Задание `hand-pose` кладёт
поверх слой с маской ТОЛЬКО на пальцы обеих рук и берёт сжатую кисть из
`Armed-Idle` набора ExplosiveLLC. Вес слоя поднимает показ оружия — без
клинка в руке герой не ходит с кулаками. Второй путь (сменить всю пластику
героя на вооружённый набор `Armed-Idle/Walk/Run`) отвергнут: он меняет всё, как
герой стоит и ходит, ради одной кисти.

Щупы: `hand-probe` (кости кисти, зеркальность, пальцы в системе кисти),
`grip-probe` (восемь вариантов в ряд в БОЕВОЙ позе, три кадра — ряд и крупный
план с двух сторон).

### Звук

**Гриб.** Своих звуков у набора InfinityPBR нет ни одного файла — проверено
по всей папке. Промты написаны в `Войс/sound-list.md` (пробуждение, замах,
урон, смерть, холостой), генерит Павлон. Гнёзда готовы: имена
`mush_wake_1`, `mush_attack_1`, `mush_hurt_1`, `mush_death_1`,
`mush_idle_1` — как только файлы лягут в `Assets/_Game/Audio/Voices`,
заиграют сами. Пробуждение играет `AmbushSleeper` в момент подъёма из
засады, а не мозг зверя при захвате цели: к захвату гриб уже встал, и
«оказался живым» прозвучало бы после того, как это стало видно.

**Волчий вой подвешен — поводом стало занятие.** У волка в наборе есть клип
`Howl`. Значит вой это не звук сам по себе, а занятие: волк спокоен, никого
не видит, садится и воет. Компонент `RestVoice` вешает голос на любое
занятие любого зверя. Слышимость воя 90 м против общих 28 — вой на то и вой,
что доносится издалека; дальность теперь задаётся при каждом
воспроизведении, иначе один дальний звук оставил бы свою слышимость
следующему в пуле.

**Урон и смерть не звучали ни у кого** — `CreatureVoice`. Файлов пока нет,
промты дописаны, гнёзда: `wolf_hurt_1`, `wolf_death_1`, `boar_hurt_1`,
`boar_death_1`.

**Хрюканье кабана в проекте ровно одно.** Кабанов десять, хрюкают раз в семь
секунд — один клип слышен как заевший сэмпл с третьего повтора. Нужно ещё
три.

### Новые задания

`beast-anims` (полные анимации существам), `beast-probe` (щуп: контроллеры,
серии ударов, занятия, голоса, пустые наборы банка), `grip-fit` (пересчёт
хвата по матрице из Blender плюс перевод кинжалов на утверждённую модель).

### Что осталось из этого захода

1. **Файлы звуков** — девять генераций у Павлона (пять грибу, четыре на урон
   и смерть зверям) плюс три хрюканья кабану.
2. **`forest.wav`** (14.5 МБ) лежит в `Войс/` неиспользованным — похоже,
   новый фон места вместо `forest_birds.ogg`. Не трогал, это его решение.
3. **Встряхивание и «озирается» у кабана** взяты не все: клипы `BIdle
   SmallShake` и `BIdle Big Shake` требуют своего повода (перебивка стойки),
   механики для неё нет.
4. **Хват общий на весь арсенал.** Числа сняты с кинжала; меч или топор
   сядут иначе. Правильный ответ — смещение и доворот в самом описании
   предмета, см. память `short-sword-ready`.
