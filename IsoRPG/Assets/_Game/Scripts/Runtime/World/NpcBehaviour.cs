namespace IsoRPG.World
{
    /// <summary>
    /// Общие правила поведения НПС — одно место на всех.
    ///
    /// Заведено 01.09.2026 по требованию Павла: «это правило для поведения
    /// ВСЕХ НПС, оно общее должно быть, а не каждого отдельно настраивать».
    /// Повод был наглядный: поворот к игроку жил в трёх компонентах порознь
    /// (QuestGiver, NpcTurnToHero, FacePlayer), я выключил его в одном — и для
    /// Павлона ничего не изменилось.
    ///
    /// Сюда же складывать всё, что описывает поведение НПС «вообще»: каждая
    /// такая настройка, размазанная по компонентам, однажды снова разойдётся.
    /// </summary>
    public static class NpcBehaviour
    {
        /// <summary>
        /// Поворачиваются ли НПС лицом к подошедшему герою.
        ///
        /// Выключено. Это ММО: к одному НПС подойдут двое, и следить ему не за
        /// кем — он начнёт метаться между ними. Стоит так, как поставил
        /// дизайнер.
        /// </summary>
        public const bool FaceHero = false;
    }
}
