# IsoRPG — состояние

Обновлено 29.08.2026. Версия 0.7.0, точка сохранения `save-22`.

## Что это

Изометрическая РПГ на Unity 6.5 (6000.5.9f1), URP 17. Игра называется
**Adventures of Zhenya**. Репозиторий `OXBOXRU25/IsoRPG`, ветка `main`.

## Как я работаю с проектом

**Unity гоню я сам, Павлон только смотрит результат.** Список «нажми то,
потом это» — откат, а не норма.

```
node ~/.claude/scripts/unity-compile.mjs "D:/GAME Ai/IsoRPG"     # проверка кода, 3 с
```

Пакетный прогон заданий (редактор должен быть закрыт):

```
printf 'задание\n' > IsoRPG/_pending-tasks.txt
"C:/Program Files/Unity/Hub/Editor/6000.5.9f1/Editor/Unity.exe" -batchmode -quit \
  -projectPath "D:/GAME Ai/IsoRPG" \
  -executeMethod IsoRPG.EditorTools.BatchRunner.Run \
  -logFile "D:/GAME Ai/unity-batch.log"
```

**Собрал — сразу запусти сам**, доведи до арены (Enter в меню) и оставь
работать, пока Павлон не закроет. Фокус на окно наводить ДО нажатия Enter,
иначе оно уходит в чужое окно.

**Сборка занимает файл игры.** Пока игра открыта, пересобрать нельзя.

## Задания очереди

Мир: `relief` (рельеф + пересев луга + навигация), `pond` (водоёмы),
`meadow-synty` (посев луга), `meadow-ground` (земля со слоями и тропами),
`terrain-repair` (пересоздать карту высот, если данные террейна пропали),
`build` (собрать игру).

Мобы и герой: `hero-num` (поставить героя по файлу выбора), `anims-native`
(родные клипы Synty в наш контроллер), `wolves` (стая волков).

Щупы: `nav-hole` (карта дыр в навигации вокруг точки), `grip-probe` (хват
оружия вариантами в ряд), `weight` (состав сцены числами), `nav` (сетка),
`sky-probe`, `pond-probe`, `pnb-analyze` и `pnb-meadow` (разбор демо-сцен
Synty на числа), `pond-shot` (кадры из редактора).

Прочее: `hud` (вернуть интерфейс герою), `audio` (вернуть звуковой узел),
`minimap`, `music`, `synty-sky`, `menu`.

**Порядок для мира строгий: `relief` → `pond` → `meadow-ground` → `build`.**
Копка чаши не идемпотентна: каждый запуск `pond` вычитает глубину заново,
и пруды уходят всё глубже. Рельеф надо лепить начисто перед копкой.

## Мир сейчас

Луговой биом Synty. Числа сняты щупом с демо-сцен автора, не выдуманы.

- **рельеф**: перепад 20 м, крутизна средняя **11.9°**, наибольшая **48.6°
  в точке (47, −19)**. Три слоя шума (60 / 20 / 6 м), маска плоскостей даёт
  около 40% ровных мест, одна доминанта на горизонте. Мелкий слой раньше
  дублировал средний (15.3 против 17 м) — отсюда была «одеяльная» карта;
- **водоёмы четыре**: (20, −16) гладь 13, (−46, 34) гладь 29, (60, 58)
  гладь 35, (−62, −66) гладь 36. Берег гуляет заливами: край чаши
  колеблется от 1.18 радиуса глади до полной чаши. Уже глади не станет
  никогда, иначе вода вылезет на сушу — модель глади круглая;
- **стая волков** у малого пруда, три штуки. Собраны в три этажа: корень
  с агентом → узел наклона (`GroundAlign`) → модель с аниматором;
- **земля**: шесть слоёв лугового биома, плитка 2–4 м. Доли автора: трава
  80.5%, палая листва 6.5%, земля 2.1%, галька 2.0%, трава с листвой 1.0%,
  тропа 0.2%;
- **тропы**: слоем террейна, три извилистых линии шириной 2.5 м, огибают
  водоёмы;
- **растительность**: 1628 растений 18 видов, 6116 объектов с LOD-узлами;
- **водоёмы**: малый (20, −16) гладь 13 м, большой (−46, 34) гладь 29 м.
  Чаша копается, потом наливается вода, кувшинки только на глади;
- **небо**: купол `SM_Env_Skydome_01` с материалом `Skybox_Mat_02`, два
  облачных кольца, облака идут. Купол на слое `Sky`, выведен из-под солнца.

Сцена весит около 5 МБ (была 151 МБ до чистки).

## Что не доделано

0. **ДЫРА В НАВИГАЦИИ — следующая задача.** Герой упирается в невидимую
   стену около (44, −21), а волк там ходит: зверь ищет путь и обходит дыру,
   игрок правит клавишами и утыкается в край сетки. Щуп `nav-hole` напечатал
   карту: дыр 13 из 169, полоса по x 46–50, z −9…−21 — вдоль каменистой
   тропы. Причина: наибольшая крутизна **48.6°** в точке (47, −19), а сетка
   не строится круче **45°** (`agentSlope` в `ProjectSettings/NavMeshAreas.asset`).

   **Попытка 29.08.2026 отвергнута заказчиком — не повторять в том же виде.**
   Сделали по образцу WoW, который принёс Павлон: дно выположили до 32°
   (`LimitSlope`), по краю подняли кайму холмов шириной 40 м и высотой 24 м
   с кубическим профилем (`AddRim`). Числа вышли правильные — на дне ноль
   мест круче 45°, наибольшая упала с 48.6° до 40.2°, — а мир нет. Павлон:
   «мир стал плоским и появилось реально ощущение, что ты в чаше находишься».

   **Причина в масштабе, а не в исполнении.** В WoW зона — километры, игрок
   видит одну гряду вдалеке, и она читается как горизонт. У нас карта 400 м
   и камера изометрическая: кольцевой вал виден со всех четырёх сторон
   разом, и это читается как борт миски. Плюс выполаживание шло по ВСЕЙ
   карте, а не по проблемной полосе, и вместе с выбросами ушёл характер
   холмов — отсюда «плоский».

   Код остался в git (откачен из рабочей копии), методы `LimitSlope`,
   `Relax` и `AddRim` при возврате брать оттуда, а не писать заново.

   **Куда идти дальше:** сглаживать не всю карту, а только полосу x 46–50,
   z −9…−21; границу мира, если понадобится, делать асимметрично — гряда с
   одной-двух сторон, а не замкнутым кольцом.

1. **Трава тонет на склонах.** Своё утопление (по авторскому числу) и
   поправка на склон складываются без общего предела и вместе перекрывают
   высоту куста. Нужен предел на СУММУ: около 0.45 высоты для растений,
   0.25 для камней.
2. **Угол водной глади вылезает из-под земли.** Модель воды у Synty
   неровная, её дальние выступы уходят за выкопанную чашу. Нужно копать
   полную глубину заведомо шире глади — но осторожно, 29.08 я на этом
   сломал карту высот целиком.
3. **Мерцание тени** на близкой камере — старая жалоба, см. память проекта.
4. **Лицензия музыки**: на первом экране стоит строка про Kevin MacLeod
   (CC BY 4.0), а музыка теперь другая. Строку надо менять, лицензия нового
   набора неизвестна — ждёт решения Павлона.

## Наборы

**SIDEKICK — конструктор персонажей.** Поставлен 29.08.2026: стартовый
набор из `H:\2\Hero`, плюс инструмент и база, скачанные с GitHub Synty
(`SyntyStudios/SidekicksToolRelease`, копии лежат там же в `H:\2\Hero`).
Открывается меню `Synty → Sidekick Character Tool`. Частей всего 58 — два
набора, Fantasy Knights и Sci-fi Civilians; остальные восемь наборов в базе
числятся, но мешей к ним нет, это платные паки. Собранный герой
выгружается кнопкой `Export Character as FBX` и попадает своей папкой в
корень `Assets`. Сборка склеивается в ОДИН меш: снять оружие или часть
из готовой модели нельзя, только пересобрать в конструкторе и выгрузить
заново.

**Герой сейчас — `Human-Custom2`** (`Assets/Human-Custom2`), собран
Павлоном в Sidekick. Рецепт сборки лежит рядом файлом `.sk`, из него
герой восстанавливается кнопкой `Load Character`.

Установлены и в `.gitignore` (восстанавливаются из `H:\1` и `H:\2`
командой `node tools/import-unitypackage.mjs <путь>`):

- **PolygonNatureBiomes** — заколдованный лес, луговой, болота, тропики.
  938 МБ. Внутри лежал вложенный вариант под URP
  (`URP_ExtractMe/Unity_2022.unitypackage`), поставлен поверх;
- COZY 3 (небо и погода) — 517 МБ, встроенный пакет в `Packages/`. **На
  второй машине его надо доставить руками**, иначе Package Manager ругнётся
  на неразрешённую зависимость. Сейчас в сцене не используется — небо
  сделано куполом Synty;
- Blink, ExplosiveLLC, GSpawn, Layer Lab, PolysplitGames, VFX_Klaus.

**Правка чужих шейдеров.** В `PNB_Core/Shaders` у `SyntyStudios_WaterShader`
и `WaterScrolling` снято семь объявлений `_CameraDepthTexture_TexelSize` в
каждом: URP 17 объявляет её сам, и своё объявление ломало компиляцию. При
переустановке набора правку придётся повторить.

## Звук и музыка

- банк звуков Kenney (CC0): бой, шаги, интерфейс, предметы, голоса, птицы;
- заставка — `Action 3 Loop`, зациклена;
- в игре — десять дорожек `Ambient_*` вперемешку из `Resources/Music`,
  пауза между темами 6 с, вход и выход плавные;
- слушатель звука **на герое**, не на камере: в изометрии камера в двадцати
  метрах, и звук рядом с героем доходил бы десятой долей.

## Грабли, на которые уже наступали

- **Строитель СТАРОЙ песочницы** вешал на героя миникарту, настройки,
  панель кнопок, подсказки, журнал, шаги и слушателя, а также собирал
  звуковой узел. Арену собирал другой строитель — всё это приходилось
  возвращать по частям заданиями `hud`, `audio`, `minimap`;
- **`SaveOpenScenes` пишет только открытые сцены.** Задание правит арену,
  следующее задание в том же прогоне открывает другую сцену — правка
  арены теряется. Разводить по отдельным прогонам;
- **выключить объект недостаточно**: `SetActive(false)` гасит отрисовку и
  скрипты, но объект остаётся в файле и создаётся при загрузке;
- **шейдер судить по тегам, а не по имени**: `SyntyStudios/SkyboxUnlit`
  объявлен как обычная геометрия, в `RenderSettings.skybox` не встаёт;
- **одна правка — один взгляд.** 29.08 три правки в одном коммите скрыли,
  какая из них сломала карту высот. Откат сломал ещё больше.
